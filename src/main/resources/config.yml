server:
  applicationConnectors:
    - type: http
      port: 8081
  adminConnectors:
    - type: http
      port: 8082

database:
  driverClass: org.postgresql.Driver
  url: jdbc:postgresql://localhost:5432/testdb
  user: postgres
  password: postgres

logging:
  level: INFO